checkout:
  post:
    - git submodule sync
    - git submodule update --init

dependencies:
  override:

test:
  override:
    - scripts/install.sh $(cat deps/*)

general:
  artifacts:
    - config.log
    - test/*.log
    - Makefile
